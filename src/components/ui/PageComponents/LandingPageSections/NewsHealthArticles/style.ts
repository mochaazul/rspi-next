import { colors } from '@/constant';
import styled from 'styled-components';

export const WrapperNewsHealthArticles = styled.div`
  .slider-title {
    & > .see-all {
      color: ${ colors.paradiso.default };
    }
  }

`;